# v17 Report

Date: 2026-03-15

## Session Progress
- Session progress: 2/15

## Current State Analysis
- The config now defined a stronger committee roster, but the renderer still exposed only the legacy role list, which meant operators and future scripts could not see the new council and evaluator contracts.
- Without a rendered V2 brief, the stronger architecture existed only in config and validation logic instead of becoming usable review input.

## Research
- The new V2 roster now exists in config, but render-committee.py still only exposes the legacy roles list, so operators cannot see the council, secretariat, or evaluator contracts.
- The next useful step is to add V2 summaries in common.py and render them in both text and JSON output without removing the compatibility view.
- The review brief becomes much more actionable once each council and persona includes expected output fields, because later iterations can reuse those contracts in capture and evaluation.

## Committee Review
- Product Manager Committee: Challenge user value, scope discipline, sequencing, and acceptance criteria before implementation starts. Members: Maya Chen, Idris Kapoor, Elena Park
- Technical Architect Committee: Challenge architecture, interfaces, migration cost, validation strategy, and operational safety. Members: Victor Hale, Priya Raman, Jonas Meyer
- User Committee: Challenge the plan from concrete usage scenarios, day-to-day friction, trust, and clarity. Members: Leo Zhang, Sofia Martinez, Ben Carter
- Product council wants the rendered brief to show what each council is supposed to decide, not just who is nominally present.
- Architecture council wants one shared summary path so future scripts reuse the same council and persona metadata instead of re-parsing config ad hoc.
- Operator council wants the richer output to stay readable, with a compact brief rather than a wall of persona prose.
- Add shared council, secretariat, evaluator, and persona summary helpers and teach render-committee.py to emit a V2 review brief alongside the legacy role summary.

## Version Goal
- Goal: Render council responsibilities and output contracts from config
- render-committee.py outputs council, secretariat, and evaluator responsibilities.
- Rendered output includes expected fields instead of only names.
- The output can be used directly as a review brief.

## Key Observations
- Shared summary helpers in common.py were enough to keep the richer render path config-driven and reusable without forcing a second config parser into render-committee.py.
- Keeping the legacy roles section in the output preserved continuity for existing users while the V2 brief added the higher-signal contracts needed for later capture and evaluation work.

## Evidence Sources
- .agent-loop/scripts/common.py
- .agent-loop/scripts/render-committee.py
- .agent-loop/scripts/validate-kit.py
- Durable review state: `.agent-loop/state.json`
- Project data snapshot: `.agent-loop/data/project-data.json`
- Project data quality: `.agent-loop/data/data-quality.json`

## Data Quality
- Project data remained ready while committee rendering grew from roster output into a usable review brief.
- Latest quality status: `ready`, score `100`

## Delivered
- Added shared helpers that summarize persona catalog, councils, secretariat, and evaluator config.
- Updated render-committee.py to emit a V2 review brief in both JSON and human-readable text output.
- Extended validate-kit so the renderer must expose council, secretariat, and evaluator summaries successfully.

## Full Validation
- `python3 .agent-loop/scripts/validate-kit.py` -> PASS (exit 0)

## Reflection
- Rendering contracts mattered more than adding more personas because the kit only gets stronger when operators and later scripts can see what each role is expected to produce.
- Backward-compatible output is still the right pattern here: strong new structure should layer on top of the old path until downstream scripts are upgraded.

## Proposed Next Goal
- Upgrade the durable review-state schema so it can hold councils, scope decisions, evaluation, and escalation cleanly without abandoning backward compatibility.
