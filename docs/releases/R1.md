# R1 Release Report

Date: 2026-03-16

## Release Definition
- Release: R1
- Title: R1: Make publish transactions persist committed loop state without dirty post-publish drift
- This release repairs the publish transaction contract so shipped versions no longer leave release-closeout state drifting in the worktree.
- It makes successful publish paths commit their own durable state and keeps failed publish-target validation from silently consuming active work.

## PM Release Brief
- Archetype: workflow
- Objective: Make publish transactions durable and deterministic so successful publishes leave no dirty runtime state behind.
- Target user value: Operators can trust that a published task or release leaves the repo in a clean, auditable state and that failed publish attempts do not silently consume work.
- Why now: Publish ordering is the highest-risk review finding because it can corrupt session progress and release history at the exact moment the kit claims a version has shipped.
- Packaging rationale: Iteration publish ordering, release closeout ordering, and validator coverage belong in one release because they are all part of the same publish transaction contract.
- Launch story: This release makes the loop trustworthy at the final moment that matters most: when a version is being published.
- Packaging signal: Archetype: workflow
- Packaging signal: Matched keywords: release
- Packaging signal: Shared theme: without
- Packaging signal: Bundled goals: 1

## Included Scope
- Make publish transactions persist committed loop state without dirty post-publish drift

## Release Scope Boundary
- In scope: Preflight publish targets before mutating loop state or backlog.
- In scope: Persist successful iteration and release closeout state inside the committed artifact.
- In scope: Extend validator coverage for clean post-publish state and failed publish rollback expectations.
- Out of scope: Do not change release planning semantics or committee structure.
- Deferred: Reset new sessions cleanly and accept structured review-state artifacts as valid gate input
- Deferred: Harden usage-log analysis and align doctor diagnostics with real publish blockers
- Deferred: Expand validator coverage for reliability fixes and publish a final remediation review

## Requirement Delivery
- v62 Make publish transactions persist committed loop state without dirty post-publish drift: Reordered publish-iteration.py so remote checks happen before durable state mutation and failed publish-target validation cannot consume the active goal.
- v62 Make publish transactions persist committed loop state without dirty post-publish drift: Reordered publish-release.py so release history, session closeout, and usage events are saved before commit, allowing successful release publish to leave a clean worktree.
- v62 Make publish transactions persist committed loop state without dirty post-publish drift: Extended validate-kit.py to assert clean post-release state and to verify failed iteration publish attempts do not advance progress or complete backlog items.

## Release Acceptance
- publish-iteration.py does not consume a goal before publish-target validation succeeds.
- publish-release.py leaves a clean worktree after a successful release publish.
- validate-kit.py proves both guarantees.

## Technical Validation
- v62 Make publish transactions persist committed loop state without dirty post-publish drift: `python3 .agent-loop/scripts/validate-kit.py` -> PASS (exit 0)

## Detailed Output
- Iteration publish now validates remote requirements before mutating session state or backlog, so invalid publish targets fail cleanly.
- Release publish now records release history and session closeout before commit, allowing a successful release publish to leave the repo in a clean post-publish state.
- Validator coverage now checks both the successful clean-worktree path and the failed-publish non-consumption path.
- v62 Make publish transactions persist committed loop state without dirty post-publish drift: The release-closeout dirty-state bug came from a simple ordering mistake rather than a missing data model.

## Task Traceability
- v62 Make publish transactions persist committed loop state without dirty post-publish drift -> `docs/reports/v62.md`

## Session Progress
- Releases completed after publish: 1/4
- Task iterations included: 1

## Next Release Recommendation
- Reset transient review and failure state for fresh sessions, then align review gates with the structured committee schema.
