# R2 Release Report

Date: 2026-03-16

## Release Definition
- Release: R2
- Title: R2: Reset new sessions cleanly and accept structured review-state artifacts as valid gate input
- This release makes a fresh loop session behave like a truly fresh run instead of inheriting stale review pressure from the previous one.
- It also aligns gate enforcement with the structured committee schema that the kit already persists as durable state.

## PM Release Brief
- Archetype: workflow
- Objective: Make every new session start cleanly and let the real structured review schema satisfy loop gates.
- Target user value: Operators can start a fresh remediation run without inherited failure pressure, and structured committee artifacts work without needing legacy flat review fields.
- Why now: After publish ordering is fixed, the next biggest trust issue is stale review and failure state leaking into fresh sessions and gates.
- Packaging rationale: Session reset, history scoping, and structured review-content checks belong together because they all define what the loop considers active state for the current session.
- Launch story: This release makes a new loop session actually feel new, while keeping the stronger structured committee model first-class.
- Packaging signal: Archetype: workflow
- Packaging signal: Matched keywords: release
- Packaging signal: Shared theme: valid
- Packaging signal: Bundled goals: 1

## Included Scope
- Reset new sessions cleanly and accept structured review-state artifacts as valid gate input

## Release Scope Boundary
- In scope: Reset transient review and failure state when starting a fresh session.
- In scope: Scope escalation history to the current session.
- In scope: Treat structured research, council, secretariat, scope, and evaluator fields as valid review content.
- Out of scope: Do not redesign escalation policy or release planning semantics.
- Deferred: Harden usage-log analysis and align doctor diagnostics with real publish blockers
- Deferred: Expand validator coverage for reliability fixes and publish a final remediation review

## Requirement Delivery
- v63 Reset new sessions cleanly and accept structured review-state artifacts as valid gate input: Added structured review-content detection so research gates, councils, secretariat outputs, scope decisions, evaluator results, and escalation records all count as real review state.
- v63 Reset new sessions cleanly and accept structured review-state artifacts as valid gate input: Scoped escalation history to the current session id and started recording session ids in published iteration history.
- v63 Reset new sessions cleanly and accept structured review-state artifacts as valid gate input: Reset fresh sessions with a full normalized release shape, cleared transient review state, and reset consecutive failure pressure.
- v63 Reset new sessions cleanly and accept structured review-state artifacts as valid gate input: Extended validator coverage for fresh-session reset behavior and structured review-content compatibility.

## Release Acceptance
- A fresh session clears transient review and failure pressure.
- Structured review-state content satisfies readiness and reporting gates without legacy flat lists.
- validate-kit.py covers both behaviors.

## Technical Validation
- v63 Reset new sessions cleanly and accept structured review-state artifacts as valid gate input: `python3 .agent-loop/scripts/validate-kit.py` -> PASS (exit 0)

## Detailed Output
- set-loop-session.py now resets transient review and failure state while preserving the normalized release shape for the next run.
- Escalation history now scopes itself to the active session id, and published iteration history records that session id explicitly.
- Structured review artifacts such as research gates, scope decisions, secretariat outputs, and evaluator results now satisfy review-state gates directly.
- v63 Reset new sessions cleanly and accept structured review-state artifacts as valid gate input: The gate bug came from schema drift: storage evolved to structured review artifacts, but the emptiness check still only trusted legacy flat arrays.

## Task Traceability
- v63 Reset new sessions cleanly and accept structured review-state artifacts as valid gate input -> `docs/reports/v63.md`

## Session Progress
- Releases completed after publish: 2/4
- Task iterations included: 1

## Next Release Recommendation
- Harden usage-log analysis against malformed rows and make doctor surface the same remote blockers that publish scripts really enforce.
