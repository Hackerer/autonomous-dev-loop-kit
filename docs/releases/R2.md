# R2 Release Report

Date: 2026-03-16

## Release Definition
- Release: R2
- Title: R2: Make release state transitions deterministic and block orphan publish paths
- This release hardens the loop's state machine so release history and publish attribution stay trustworthy.

## PM Release Brief
- Objective: Make release sequencing deterministic and prevent orphan task publishes.
- Target user value: Operators can trust release numbering, session progress, and publish attribution without reading raw state.
- Why now: Telemetry is only useful if release and publish state cannot drift or produce orphan records.
- Packaging rationale: Release sequencing fixes and orphan guards belong together because both protect the trustworthiness of release history and usage telemetry.
- Launch story: This release hardens the loop's state machine so every published task and release has a trustworthy place in session history.

## Included Scope
- Make release state transitions deterministic and block orphan publish paths

## Release Scope Boundary
- In scope: Session-id derivation for active sessions
- In scope: Consecutive release numbering within a session
- In scope: Active-goal and active-release guards before reporting and publish
- Out of scope: Placeholder report quality gates
- Out of scope: Status and doctor commands
- Deferred: Turn task and release reports into trustworthy publish artifacts
- Deferred: Strengthen PM release planning with archetypes and better packaging signals
- Deferred: Add operator-facing status and doctor tooling for loop recovery

## Requirement Delivery
- v58 Make release state transitions deterministic and block orphan publish paths: Added session-id derivation for active sessions that were started before explicit session ids were persisted.
- v58 Make release state transitions deterministic and block orphan publish paths: Kept next-release numbering tied to the active session while still recovering from dirty release counters via session-local release history.
- v58 Make release state transitions deterministic and block orphan publish paths: Blocked orphan report and publish attempts when the active goal is unspecified or detached from the planned release.

## Release Acceptance
- Current sessions derive a stable session id even after migration
- Release planner keeps consecutive numbers within the active session
- Orphan iteration publish attempts fail with actionable errors

## Technical Validation
- v58 Make release state transitions deterministic and block orphan publish paths: `python3 .agent-loop/scripts/validate-kit.py` -> PASS (exit 0)

## Detailed Output
- Migrated sessions now derive stable session ids, which keeps telemetry coherent even when a session started on an older kit build.
- Orphan report and publish attempts now fail before they can poison release history or usage logs.
- v58 Make release state transitions deterministic and block orphan publish paths: The most important consistency fix was session-local: global release history is not a safe source of truth for a newly started session.
- v58 Make release state transitions deterministic and block orphan publish paths: Guarding report/publish at the active-goal boundary prevents corrupted history much earlier than downstream report analysis can.

## Task Traceability
- v58 Make release state transitions deterministic and block orphan publish paths -> `docs/reports/v58.md`

## Session Progress
- Releases completed after publish: 2/5
- Task iterations included: 1

## Next Release Recommendation
- Next, block placeholder task and release notes so every published report is concrete and trustworthy.
