# R5 Release Report

Date: 2026-03-16

## Release Definition
- Release: R5
- Title: R5: Add operator-facing status and doctor tooling for loop recovery
- This release packages the operator recovery surface for the loop: one script to inspect live session state and one script to explain blockers and the next recovery action.
- It turns the evidence from recent usage-log analysis into day-to-day operator tooling without expanding the protocol or validation model.

## PM Release Brief
- Archetype: workflow
- Objective: Give operators a first-class way to inspect loop state and recover from blockers.
- Target user value: Operators can answer where the loop is stuck and what to run next without reading raw state files.
- Why now: After telemetry, state consistency, report trust, and PM release planning quality, the main UX gap is day-to-day recovery and inspection.
- Packaging rationale: Status and doctor belong together because one explains current loop state while the other explains how to recover.
- Launch story: This release gives operators a practical cockpit for the loop: one command to inspect state, one command to recover.
- Packaging signal: Archetype: workflow
- Packaging signal: Matched keywords: operator, release
- Packaging signal: Shared theme: tooling
- Packaging signal: Bundled goals: 1

## Included Scope
- Add operator-facing status and doctor tooling for loop recovery

## Release Scope Boundary
- In scope: Add loop-status.py
- In scope: Add loop-doctor.py
- In scope: Document status and doctor in README, protocol, and installer guidance
- Out of scope: No new validation or committee gates

## Requirement Delivery
- v61 Add operator-facing status and doctor tooling for loop recovery: Added loop-status.py to surface the active session, release, goal, review, gate, validation, and draft state.
- v61 Add operator-facing status and doctor tooling for loop recovery: Added loop-doctor.py to diagnose missing session setup, missing releases, goal mismatches, review gaps, validation failures, and remote configuration issues.
- v61 Add operator-facing status and doctor tooling for loop recovery: Extended validate-kit.py, README.md, protocol guidance, and installer bootstrap output to cover operator recovery commands.

## Release Acceptance
- loop-status.py prints session, release, goal, and gate status
- loop-doctor.py prints actionable recovery findings
- validate-kit.py covers both scripts

## Technical Validation
- v61 Add operator-facing status and doctor tooling for loop recovery: `python3 .agent-loop/scripts/validate-kit.py` -> PASS (exit 0)

## Detailed Output
- loop-status.py now prints the active session id, release progress, selected goal, review status, implementation gate outcome, validation state, and draft artifacts in one compact view.
- loop-doctor.py now emits structured findings for missing session setup, missing bundled releases, goal and release mismatches, review-state gaps, implementation-gate blockers, validation failures, missing release reports, and missing git remotes.
- The release also updates validation, README guidance, protocol guidance, and installer bootstrap output so projects that install the kit get the new operator recovery workflow immediately.
- v61 Add operator-facing status and doctor tooling for loop recovery: The implementation gate and publish flow already encoded enough durable state to support a useful operator cockpit without adding new protocol phases.

## Task Traceability
- v61 Add operator-facing status and doctor tooling for loop recovery -> `docs/reports/v61.md`

## Session Progress
- Releases completed after publish: 5/5
- Task iterations included: 1

## Next Release Recommendation
- Use the improved telemetry plus status and doctor tooling to observe real sessions, then simplify the highest-friction prompts and release-planning steps from evidence.
